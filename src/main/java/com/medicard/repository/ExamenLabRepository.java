package com.medicard.repository;

import com.medicard.models.ExamenLab;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExamenLabRepository extends JpaRepository<ExamenLab, Integer> {
}
